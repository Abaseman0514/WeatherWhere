"use strict"; {
    angular.module('app')
        .controller('userPreferencesController', function (weatherService) {
            const $ctrl = this;

            // $ctrl.newSearch = weatherService.newSearch;


        });

}